apiVersion: v1
kind: Service
metadata:
    name: bipeensinhaazuredevopskubernetes
spec:
    type: LoadBalancer
    ports:
    - port: 8080 
    selector:
        app: bipeensinhaazuredevopskubernetes